[build]
  command = "npm run build"
  publish = "dist"

# Single-page-app fallback: every route serves index.html so React Router
# handles it (otherwise refreshing /job/123 would 404 on Netlify).
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
